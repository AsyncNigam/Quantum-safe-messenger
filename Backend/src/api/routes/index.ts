import keyRoutes    from './keyRoutes';
import healthRoutes from './healthRoutes';
import authRoutes   from './authRoutes';

/**
 * Central export point for all API routes.
 */
export { authRoutes, keyRoutes, healthRoutes };
